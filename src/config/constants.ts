
export const SERVER_NAME = "microservice_socket";

export const MEMORY_LIMIT = "128Mb";

export enum RESPONSE_CODE {
    SUCCESS = 200,
    CREATED = 201,
    INVALID = 400,
    UNAUTHORIZED = 401,
    FORBIDDEN = 403,
    ERROR = 500
}

